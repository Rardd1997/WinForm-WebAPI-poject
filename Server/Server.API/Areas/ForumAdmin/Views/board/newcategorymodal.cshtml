@model mvcForum.Web.Areas.ForumAPI.ViewModels.Category
@{
	String ns = "MVCForum.ForumAdmin.Board.Index";
}
	<div id="newcategorymodal" class="modal hide" data-bind="css: { hide: AddHidden }">
		<div class="modal-header">
			<button type="button" class="close" data-bind="click: HideAdd">&times;</button>
			<h3>@Html.LocalizedString("AddCategoryHeading", ns)</h3>
		</div>
		<!-- ko with: CreateCategoryModal -->
		<div class="modal-body">
			@using (Html.BeginForm("create", "category", new { area = "forumadmin", id = Model.Id })) {
				<fieldset>
					<legend class="hide"></legend>
					<div class="control-group" data-bind="css: { error: !Name.isValid() }">
						@Html.LabelFor(x => x.Name, new { @class = "control-label" })
						<div class="controls">
							@Html.TextBoxFor(x => x.Name, new { id = "categoryname", @class = "input-large", data_bind = "value: Name" })
						</div>
					</div>
					<div class="control-group" data-bind="css: { error: !SortOrder.isValid() }">
						@Html.LabelFor(x => x.SortOrder, new { @class = "control-label" })
						<div class="controls">
							@Html.TextBoxFor(x => x.SortOrder, new { @class = "input-large", data_bind = "value: SortOrder" })
						</div>
					</div>
				</fieldset>
			}
		</div>
		<div class="modal-footer">
			<a href="#" class="btn" data-bind="click: $parent.HideAdd">@Html.LocalizedString("AddCategoryClose", ns)</a>
			<a href="#" class="btn btn-primary" data-bind="click: Create">@Html.LocalizedString("AddCategoryCreate", ns)</a>
		</div>
		<!-- /ko -->
	</div>
